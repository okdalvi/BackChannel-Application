<li>
  <%= gravatar_for user %>
  <%= link_to user.name, user %>

  <% if current_user.admin == 2 %>
      <% if user.admin!=2 %>

<%# if current_user.admin ==1 && !current_user?(user) && user.email != "superadmin@liveforum.com" %>
    | <%= link_to "Delete", user, method: :delete,
                                  data: { confirm: "You sure?" } %>
      <% if user.admin == 1 %>
        | <%= link_to "Revoke admin", :action => 'revoke_admin', :id => user.id%>
      <% else %>
        |  <%= link_to 'Make admin', :controller => 'users', :action => 'make_admin', :id => user.id%>
      <% end%>
      <% end %>
<% end %>

      <% if current_user.admin == 1 %>
      <% if user.admin != 1&& user.admin!=2 %>
         |  <%= link_to "Delete", user, method: :delete,
                      data: { confirm: "You sure?" } %>

          <% if user.admin != 1 && user.admin!=2 %>

              |  <%= link_to 'Make admin', :controller => 'users', :action => 'make_admin', :id => user.id%>
  <% end %>
  <% end %>
      <%end%>

</li>