<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Title %><br />
    <%= f.text_field :name %>
    <%= f.label :Category %>
    <%= select("post", "category_id", Category.find(:all).collect{ |c| [ c.title, c.id] }) %>
    <%#= f.label :category_id %>
    <%#= select("post", "category_id", Category.all.collect{ |c| [ c.title, c.id] }) %>
  </div>

    <div class="field">
    <%= f.label :Content %><br />
    <%= f.text_area :content %>
  </div>

  <div class="field">
    <%= f.label :Tag %><br />
    <%= f.text_field :tag %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
